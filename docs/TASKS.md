# Task Nhỏ Cho AI Thực Hiện Lần Lượt

1. Implement và test `AddressParser` cho `A1`, `$A$1`, `Sheet1!A1`, range và đổi tên cột.
2. Implement `TemplateLexer` cho cú pháp `{{...}}`.
3. Implement `TemplateParser.parseCell()` cho text, placeholder, helper và image.
4. Implement parse block nhiều node cho `#each`, `#each-col`, `#block`, `#if`.
5. Implement `JsonPathResolver` cho dot path, bracket index và scope hiện tại.
6. Implement `DefaultHelperRegistry` hỗ trợ helper sync/async.
7. Implement scanner ExcelJS đọc workbook thành `WorkbookTemplateSource`.
8. Implement `RenderPlanner` cho placeholder thường.
9. Implement `RenderPlanner` cho `if`.
10. Implement row loop cho `each`.
11. Implement column loop cho `each-col`.
12. Implement block clone cho `block`.
13. Implement `ExcelJsWorkbookRenderer.apply()` cho `SetCellValue`.
14. Implement clone row bằng ExcelJS.
15. Implement clone column bằng ExcelJS.
16. Implement clone block bằng ExcelJS.
17. Implement `StyleCloneManager`.
18. Implement `MergeManager`.
19. Implement `FormulaManager`.
20. Implement `ImageManager`.
21. Thêm integration test render template thật.
22. Thêm hướng dẫn publish npm và CI.
